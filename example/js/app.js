$(function() {
    MobilePages.page('home')
        .addProperty(
            'init',
            function init() {

            });

    MobilePages.page('second')
        .addProperty(
            'init',
            function init() {

            });
});